<div class="container">
  <div class="row">
    <div class="col-md-3">
      <%= render 'books/book', books: @books,user: @user %>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2>Books</h2>
      <hr>
      <table>
        <thead>
          <th>Title</th>
          <th>Opinion</th>
        </thead>
        <tbody>
          <% @books.each do |book| %>
            <tr class="book">
              <td>
                <% if book.user && book.user.profile_image.attached? %>
                  <%= link_to user_path(book.user) do %>
                    <%= image_tag book.user.profile_image, size: "100x100" %>
                  <% end %>
                <% else %>
                  <%= link_to user_path(book.user) do %>
                    <%= image_tag 'default-image', size: "100x100" %>
                  <% end %>
                <% end %>
              </td>
              <td><%= link_to book.title, book_path(book) %></td>
              <td><%= book.body %></td>
            </tr>
            <tr>
              <td colspan="3"><hr></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
